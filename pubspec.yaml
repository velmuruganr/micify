name: micify
description: Ultra-low-latency real-time microphone amplification utility.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  permission_handler: ^11.3.1
  flutter_pcm_sound: ^3.3.3
  record: ^6.2.1
  wakelock_plus: ^1.4.0
  package_info_plus: ^9.0.1
  shared_preferences: ^2.3.0
  flutter_tts: ^4.2.0
  confetti: ^0.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
